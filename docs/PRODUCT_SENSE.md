# Product sense

- **Operators** need predictable commands and safe defaults (no destructive actions without explicit flags).
- **Automation** needs JSON and stable exit codes.
- **Parity** with the web console where possible reduces training cost.
